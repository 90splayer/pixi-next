'use client';

import React, { useState } from 'react'

import { Sprite, useTick } from "@pixi/react";

const Slots = () => {

  return (
    <Sprite image={'/slots@2x.png'} x={430} y={-10} />
  )
}

export default Slots