'use client';

import React, { useState } from 'react'

import { Sprite, useTick } from "@pixi/react";

const H = () => {

  return (
    <Sprite image={'/h@2x.png'} x={40} y={0} />
  )
}

export default H